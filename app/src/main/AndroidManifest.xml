<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agroiapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/appicons"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/appicons"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".homenavigationdrawer"></activity>
        <activity android:name=".fertagribuy" />
        <activity android:name=".pestagriregbuy" />
        <activity android:name=".buyvalpassagr" />
        <activity android:name=".pestisideagri" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".sellgmap"
            android:label="@string/title_activity_sellgmap" />
        <activity android:name=".settingsagri" />
        <activity android:name=".fertilizeragri" />
        <activity android:name=".userprofiledb" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".sellagri" />
        <activity android:name=".buyagri" />
        <activity android:name=".weatherpincdpasser" />
        <activity android:name=".location" />
        <activity android:name=".weatherapi" />
        <activity android:name=".UserProfile" />
        <activity android:name=".profileupdateagri" />
        <activity android:name=".marketpriceagri" />
        <activity android:name=".agriforgotpass" />
        <activity
            android:name=".agrihome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".registeragri" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>